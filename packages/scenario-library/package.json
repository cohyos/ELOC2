{
  "name": "@eloc2/scenario-library",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsup src/index.ts --format esm --dts",
    "test": "echo No tests",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@eloc2/domain": "workspace:*",
    "@eloc2/events": "workspace:*"
  },
  "devDependencies": {
    "tsup": "^8.3.0",
    "vitest": "^3.0.0",
    "typescript": "^5.7.0"
  }
}
